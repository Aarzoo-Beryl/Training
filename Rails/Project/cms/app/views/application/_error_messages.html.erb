<% if object && object.errors.size > 0 %>
  <div id="error explanation">
    <h2><%= pluralize(object.error.size,"error") %> Prohibited this record from being saved</h2>
    <p> There were prblems with the following field</p>
    <ul><% object.error.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %></ul>
  </div>
<% end %>
